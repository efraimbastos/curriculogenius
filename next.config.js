// next.config.js - Reverted to default server mode
/** @type {import("next").NextConfig} */
const nextConfig = {
  // output: "export", // Removed static export for dynamic SaaS application

  // Optional: Add other configurations here if needed
  // reactStrictMode: true,
};

module.exports = nextConfig;

